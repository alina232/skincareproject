<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            background-color: #f4f7fb;
            margin: 0;
            padding: 0;
        }

        .search-header {
            background-color: #f1f1f1;
            text-align: center;
            padding: 20px 0;
            margin-bottom: 30px;
            border-bottom: 2px solid #ccc;
        }

        .container {
            width: 85%;
            margin: 0 auto;
        }

        .search-result-card {
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
            border-radius: 8px;
            transition: transform 0.3s ease, box-shadow 0.3s ease;
            background-color: #fff;
          
        }

        .search-result-card:hover {
            transform: translateY(-5px);
            box-shadow: 0 6px 12px rgba(0, 0, 0, 0.2);
        }

        .card-body {
            padding: 20px;
            margin: 5px;
        }

        .product-title {
            font-size: 1.25rem;
            font-weight: bold;
            color: #333;
        }

        .product-price {
            color: #28a745;
            font-weight: bold;
            margin-top: 10px;
            margin-bottom: 15px;
        }

        .no-results {
            text-align: center;
            font-size: 1.2rem;
            color: #6c757d;
        }

        .card-img-top {
            height: 250px;
            object-fit: cover;
            border-radius: 8px 8px 0 0;
        }

        .btn-primary {
            background-color: #D3A6F2;
            color: white;
            border-radius: 5px;
            padding: 10px 20px;
            border: none;
            transition: background-color 0.3s;
        }

        .btn-primary:hover {
            background-color: #D28BF0;
        }
    </style>
</head>
<body>
<%- include('header') %>
 <!-- Search Header -->
 <div class="search-header">
    <h2>Search Results for "<%= query %>"</h2>
</div>

<div class="container">
    <% if (products.length === 0) { %>
        <p class="no-results">No results found for your search. Please try again with different keywords.</p>
    <% } else { %>
        <div class="row">
            <% products.forEach(product => { %>
                <div class="col-md-4 mb-4">
                    <div class="card search-result-card">
                        <div class="card-body">
                            <h5 class="product-title"><%= product.ProductName %></h5>
                            <p class="product-price">$<%= product.Price %></p>
                            <a href="/products/<%= product.ProductId %>" class="btn btn-primary btn-block">View Details</a>
                        </div>
                    </div>
                </div>
            <% }) %>
        </div>
    <% } %>
</div>

<!-- Bootstrap JS and Popper.js (for any Bootstrap components like modals) -->
<script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.11.6/dist/umd/popper.min.js"></script>
<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.min.js"></script>


</body>
</html>